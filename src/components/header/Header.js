import React from "react";
import Navbar from "../navbar/Navbar";
const Header = (props) => {
  return (
    <header className="header">
      <Navbar />
    </header>
  );
};

export default Header;
